using System.Collections;
using UnityEngine;

namespace dhlworks.voicerecon.speechtotext
{
    /// <summary>
    /// Enum for the type of recognition to perform.
    /// </summary>
    public enum RecognitionMode
    {
        RAW = 0,
        JSON = 1,
        PLAINTEXT = 2
    }
}